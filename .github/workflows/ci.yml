name: CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths-ignore:
      - .release-please-manifest.json
      - CHANGELOG.md
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-cd-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: cpoul/pg_noty

jobs:
  ci:
    name: Build, vet, and unit test
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Check formatting
        shell: bash
        run: |
          unformatted="$(find cmd internal -type f -name '*.go' -print0 | xargs -0 gofmt -l)"
          if [[ -n "$unformatted" ]]; then
            echo "::error::The following Go files need gofmt:"
            printf '%s\n' "$unformatted"
            exit 1
          fi

      - name: Check module files
        run: go mod tidy -diff

      - name: Build
        run: go build ./...

      - name: Vet default build
        run: go vet ./...

      - name: Vet integration build
        run: go vet -tags=integration ./...

      - name: Run unit tests
        run: go test -count=1 ./...

  integration:
    name: Integration test
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Run integration tests
        run: >-
          go test -p=1 -tags=integration -count=1
          -skip '^TestTenThousandRowsStayWithinTheCommittedRegressionCeiling$' ./...

      - name: Measure trigger overhead (informational)
        if: github.event_name != 'pull_request'
        continue-on-error: true
        run: >-
          go test -tags=integration -count=1 -v
          -run '^TestTenThousandRowsStayWithinTheCommittedRegressionCeiling$' ./internal/source

  image:
    name: Validate multi-platform container
    needs:
      - ci
      - integration
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
      !startsWith(github.head_ref, 'release-please--branches--'))
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Read Go version
        id: go-version
        shell: bash
        run: |
          version="$(awk '$1 == "go" { print $2; exit }' go.mod)"
          test -n "$version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Build image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: false
          build-args: |
            GO_VERSION=${{ steps.go-version.outputs.version }}
          cache-from: type=gha,scope=pg-noty-container
          cache-to: type=gha,mode=max,scope=pg-noty-container
          pull: true
          provenance: mode=max
          sbom: true

  release:
    name: Prepare semantic release
    needs:
      - ci
      - integration
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Create or publish semantic release
        uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0
        with:
          token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
