W1_literal_secret.yaml:16:13
   14 |       signing:
   15 |         secrets:
>  16 |           - [redacted]
                    ^ warning: destination.signing.secrets should use an interpolated environment reference instead of a YAML literal; move the secret to the environment and write ${VARIABLE_NAME}
