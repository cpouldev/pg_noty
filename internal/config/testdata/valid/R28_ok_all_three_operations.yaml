# R28, accepting: the three statements the contract supports, each named once.
version: 1
database:
  url: postgres://noty:pw@db.internal:5432/noty
listeners:
  - name: order_paid
    table: public.orders
    operations:
      insert: {}
      update: {}
      delete: {}
    destination:
      url: https://hooks.example.test/order-paid
