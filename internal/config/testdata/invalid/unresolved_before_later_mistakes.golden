unresolved_before_later_mistakes.yaml:3:8
   1 | version: 1
   2 | database:
>  3 |   url: ${DATABASE_URL}
              ^ environment variable "DATABASE_URL" is not set
                set DATABASE_URL, or write ${DATABASE_URL:-default} to supply a fallback

unresolved_before_later_mistakes.yaml:9:14
   7 |     table: public.orders
   8 |     operations: [insert]
>  9 |     timeout: ${LISTENER_TIMEOUT}
                    ^ environment variable "LISTENER_TIMEOUT" is not set
                      set LISTENER_TIMEOUT, or write ${LISTENER_TIMEOUT:-default} to supply a fallback
