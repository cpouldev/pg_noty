# AC #16's bare-scalar case: neither accepted form, so the diagnostic shows both.
version: 1
database:
  url: postgres://noty:pw@db.internal:5432/noty
listeners:
  - name: order_paid
    table: public.orders
    operations: insert
    destination:
      url: https://hooks.example.test/order-paid
