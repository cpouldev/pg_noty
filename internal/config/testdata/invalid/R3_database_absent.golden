R3_database_absent.yaml:3:1
   1 | # R3's presence half, and AC #30's explicit sub-case: an absent database block is reported as
   2 | # the one required key inside it, not as a complaint about the block as well.
>  3 | version: 1
       ^ missing required key "database.url"
