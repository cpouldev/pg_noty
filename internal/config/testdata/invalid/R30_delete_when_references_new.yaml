version: 1
database:
  url: postgres://noty@db.internal/noty
listeners:
  - name: order_deleted
    table: public.orders
    operations:
      delete:
        when: "NEW.status = 'paid'"
    destination:
      url: https://hooks.example.test/order-deleted
