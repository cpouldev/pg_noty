# R22: the listeners key is required. An explicitly empty list is valid and means something
# else entirely, which R22_ok_listeners_empty_list.yaml holds.
version: 1
database:
  url: postgres://noty:pw@db.internal:5432/noty
