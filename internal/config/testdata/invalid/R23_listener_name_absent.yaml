# R23's presence half. The caret sits on the listener's own first key.
version: 1
database:
  url: postgres://noty:pw@db.internal:5432/noty
listeners:
  - table: public.orders
    operations: [insert]
    destination:
      url: https://hooks.example.test/order-paid
