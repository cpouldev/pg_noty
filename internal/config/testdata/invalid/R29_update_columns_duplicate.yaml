version: 1
database:
  url: postgres://noty@db.internal/noty
listeners:
  - name: order_paid
    table: public.orders
    operations:
      update:
        columns:
          - duplicate_column
          - duplicate_column
    destination:
      url: https://hooks.example.test/order-paid
