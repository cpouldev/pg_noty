# R29's legality half again, on the other statement a column filter cannot narrow.
version: 1
database:
  url: postgres://noty:pw@db.internal:5432/noty
listeners:
  - name: order_paid
    table: public.orders
    operations:
      delete:
        columns: [status]
    destination:
      url: https://hooks.example.test/order-paid
